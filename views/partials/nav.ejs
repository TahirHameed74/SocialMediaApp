<nav class="navbar">
    <a class="navbar-brand" href="/">
        <h2>Icy Tape</h2>
    </a>
    <div id="user-info">
        <% if(user) { %>
<!-- Example single danger button -->
<div class="btn-group">
        <button type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><span id="user"><%= user %></span></button>
        <div class="dropdown-menu dropdown-menu-right">
        <h6 class="dropdown-header">Clout: <span id="karma"><%=karma%></span></h6>
        <div class="dropdown-divider"></div>

          <a class="dropdown-item" href="/u/<%=user%>">Profile</a>
          <a class="dropdown-item" href="/settings">Settings</a>
          <a class="dropdown-item" href="/logout">Logout</a>
        </div>
      </div>
        <% } else { %>
        <a class="waves-effect waves-light btn-flat center-align login-btn-theme indigo-text modal-trigger" href="/login" id="user-settings">Sign in</a>
        <% } %>
    </div>

</nav>